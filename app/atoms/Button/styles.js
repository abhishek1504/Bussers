import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonParentStyle: {
    margin: 5,
  },
  buttonStyle: {
    backgroundColor: '#474fb9',
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 5,
    height: 40,
  },
  textStyle: {
    color: 'white',
  },
});

export default styles;
