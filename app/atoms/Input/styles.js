import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  parentView: {
    borderColor: 'black',
    borderWidth: 1,
    padding: 5,
    margin: 5,
  },
  textInputStyles: {
    height: 40,
  },
});

export default styles;
